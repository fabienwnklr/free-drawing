import type { Drawer } from "@/Drawer";
import { BaseOverlay } from "../BaseOverlay"

export class SelectOverlay extends BaseOverlay {
    constructor(drawer: Drawer) {
        super(drawer);
    }
}
